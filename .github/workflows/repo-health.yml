# bettergithub:generated-file
name: Repo Health

on:
  push:
  pull_request:

jobs:
  documentation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check recruiter-readiness docs
        shell: bash
        run: |
          test -f README.md
          test -d docs
          test -f docs/architecture.md
          test -f docs/test-plan.md
          test -f LICENSE
